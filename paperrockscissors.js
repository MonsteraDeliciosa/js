var userChoice = prompt("choose paper, rock or scissors", "");
var skynetChoice = (Math.random()) * 3;

if (skynetChoice <= 1) {
  var computerChoice = "paper";
}

if (skynetChoice <= 2 && skynetChoice > 1) {
  var computerChoice = "rock";
}

if (skynetChoice <= 3 && skynetChoice > 2) {
  var computerChoice = "scissors";
}

letsPlay = function() {
  //
}

switch(skynetChoice) {
  case
}
